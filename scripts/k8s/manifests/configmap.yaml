apiVersion: v1
kind: ConfigMap
metadata:
  name: propai-config
  labels:
    app: PropAi Sync
data:
  propai.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.propai/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "PropAi Sync Assistant",
            "workspace": "~/.propai/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # PropAi Sync Assistant

    You are a helpful AI assistant running in Kubernetes.


