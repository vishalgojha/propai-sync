import type { PropAiSyncConfig } from "../config/config.js";
export function registerPluginCliCommands(_program: unknown, _cfg?: PropAiSyncConfig): void {
  // CLI support removed; keep a no-op hook for legacy call sites.
}


