---
summary: "CLI reference for `propai clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `propai clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `propai clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `propai clawbot qr` (same behavior as [`propai qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `propai clawbot qr` -> `propai qr`


